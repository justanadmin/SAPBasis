check_db2_version() {
    db2_version=$(db2level | grep "DB2 v" | awk '{print $5}' | sed 's/..$//')

    if [[ "${db2_version:0:3}" == "v11" ]]; then
        echo "Version is v11. Need to check further."
    else
        echo "Version is not v11."
    fi
}

# Call the function
check_db2_version
